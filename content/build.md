---
# The route builder's page. It has no content: layouts/build/single.html is the
# whole page (the app is static/build/builder.js). It is a Hugo page only so the
# launch switch, builderPublic in hugo.toml, can reach its noindex tag and the sitemap.
title: "Route builder"
url: "/build/"
type: "build"
---
