{
  "name": "mazatzalhiking",
  "version": "0.0.1",
  "private": true,
  "description": "Route builder for the Mazatzal Wilderness",
  "scripts": {
    "build": "tools/build.sh",
    "dev": "tools/build.sh && wrangler dev",
    "deploy": "tools/build.sh && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
