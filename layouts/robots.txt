User-agent: *
Allow: /
# The data files are for the builder, not for indexing.
Disallow: /data/
{{ if not site.Params.builderPublic -}}
# The builder is unlisted until it opens (builderPublic in hugo.toml); it also carries noindex.
Disallow: /build/
{{ end -}}
Sitemap: {{ "sitemap.xml" | absURL }}
